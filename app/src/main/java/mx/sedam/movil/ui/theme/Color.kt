package mx.sedam.movil.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta militar SEDAM ─────────────────────────────────────────────────────
// Azul marino + dorado + blanco, con una descendencia de azules para degradados.

// Azules (de más oscuro a más claro)
val NavyAbyss  = Color(0xFF04101F) // fondo superior casi negro-azulado
val NavyDeep   = Color(0xFF071A2F)
val Navy       = Color(0xFF0C2340) // azul marino base (identidad)
val NavyMid    = Color(0xFF13385F)
val BlueSteel  = Color(0xFF1E4E7C)
val BlueGlow   = Color(0xFF2E6CA6) // acentos/resplandor

// Dorados (acento militar)
val Gold       = Color(0xFFC9A24B)
val GoldBright = Color(0xFFE4C468)
val GoldDeep   = Color(0xFF9C7B2E)

// Neutros
val White      = Color(0xFFFFFFFF)
val OffWhite   = Color(0xFFE7EEF6)
val SteelText  = Color(0xFF9FB6CE) // texto secundario / labels
val FieldFill  = Color(0x14FFFFFF) // relleno translúcido de campos
val FieldLine  = Color(0x33AFC6DE) // borde tenue de campos

val DangerRed  = Color(0xFFE5636B)
